#include <iostream>
using namespace std;
 
int main() {
	string s,t;
	cin>>s;
	cin>>t;
	bool done=true;
	for(int i=0;i<s.length();i++){
		if(s[i] != t[s.length()-i-1] ){
			done=false;
			break;
		}
	}
	if(done) cout<<"YES";
	else cout<<"NO";
	return 0;
}
