#include <iostream>
using namespace std;
 
int main() {
	int n,h;
	cin>>n>>h;
	int count=0;
	int* arr=new int[n];
	for(int i=0; i<n; i++){
		cin>>arr[i];
		if(arr[i]<=h) count++;
		else count=count+2;
	}
	cout<<count;
	return 0;
}
